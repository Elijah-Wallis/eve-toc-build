# Supabase (required for storing diagnostics)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: MCP endpoint for full Eve L5_Medspa_IaC diagnostic
# If unset, the app uses built-in mock projections
MCP_ENDPOINT=https://your-mcp-server.com/api/diagnostic
# or
NEXT_PUBLIC_MCP_ENDPOINT=https://your-mcp-server.com/api/diagnostic

# Optional: OpenAI for executive narrative (otherwise fallback text is used)
OPENAI_API_KEY=sk-...
